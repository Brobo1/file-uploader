<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Document</title>
    <link rel="stylesheet" href="/css/output.css">
</head>
<body>

<%- include("partials/header.ejs") %>

<div id="container" class="flex justify-center items-center flex-col mt-4 gap-2">
    <% if (user) { %>
        <p id="user-welcome">Welcome <%= user.username %></p>

    <% } else { %>
        <p class="text-2xl">Welcome to File uploader!</p>
        <p><a href="/signup" class="hover:underline">Sign up</a>  or <a href="/login" class="hover:underline">login</a> to start uploading files</p>
        <p>or test out the site with a <a href="/login/demo" class="hover:underline">Demo user</a></p>
    <% } %>

</div>

</body>
</html>